import { useContext, useState } from "react";
import { APIContext } from "../APIContext";

const CategorySearch = () => {
      const { API_URL, recipes, setRecipes, error, setError } = useContext(APIContext)

  return (
      
    <div>
      
    </div>
  )
}

export default CategorySearch
