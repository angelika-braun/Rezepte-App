import DarkModeButton from "./DarkModeButton"
import Navbar from "./Navbar";

const Header = () => {
  return (
  <header>
      <DarkModeButton />
      <Navbar />
 </header>    
  )
}

export default Header
